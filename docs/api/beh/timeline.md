# Behavior Timeline

Timelines own behavior runtime state, in-memory rows, optional raw JSONL
output, optional trial summary CSV output, recovery state, sync helpers, and
global researcher keys.

::: trackflow.beh.timeline
    options:
      members:
        - Timeline
        - setup_timeline
